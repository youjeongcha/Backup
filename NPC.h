#pragma once
#include "Character.h"

class NPC : public Character {
public:
    NPC();
    //NPC(const std::string& npcName, const std::vector<int>& items, const std::string& path);
    ~NPC();



    // NPC에게 특화된 함수들...
};